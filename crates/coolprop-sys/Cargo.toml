[package]
name = "coolprop-sys"
description = "Raw FFI bindings to the CoolProp C API (CoolPropLib.h)"
version.workspace = true
edition.workspace = true
license.workspace = true
links = "CoolProp"
build = "build.rs"

[build-dependencies]
cmake = "0.1"
